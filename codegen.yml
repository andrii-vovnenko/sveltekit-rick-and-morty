overwrite: true
schema: "https://rickandmortyapi.com/graphql"
documents: "src/lib/graphql/queries/*"
generates:
  src/lib/graphql/types.ts:
    plugins:
      - typescript
      - typescript-operations

